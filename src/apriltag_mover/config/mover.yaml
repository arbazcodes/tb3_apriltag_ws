apriltag_mover:
  ros__parameters:
    use_sim_time: true
    world_name: simple_world
    model_name: apriltag_marker
    bounds: 5.0        # half width in meters
    speed: 0.15        # m/s base travel speed
    update_hz: 20.0